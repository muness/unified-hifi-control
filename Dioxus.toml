[application]
name = "unified-hifi-control"
asset_dir = "public"

[web.app]
title = "Unified Hi-Fi Control"

[web.watcher]
reload_html = true
watch_path = ["src", "public"]

[web.resource.dev]
base_path = "/"

# CSS and favicon are embedded in the binary (ADR 002)
# See src/app/components/layout.rs for document::Style and document::Link with data URLs
